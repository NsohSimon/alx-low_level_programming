#include "main.h"
/**
 * _islower-tells if a letter is lower case
 * Return: 1 if lc 0 if not
 */
int _islower(int c)
{
  if (c >= 'a'; c <= 'z')
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
